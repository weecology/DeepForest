#Training module


def run(ds, model, config):
    """Train a Deepforest model in pytorch
    Args:
        ds: a pytorch dataset, see main.load_dataset
        model: a deepforest model see main.create() or main.load_model()
        config: a deepforest config object
    """

    pass
